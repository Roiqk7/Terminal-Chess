int main()
{
        // start game
        startGame();

        // game loop
        while (true)
        {
                // take user input
                Event userInput = handleUserInput();

                // handle event
                eventHandler.submit(userInput);

                // update game state
                updateGameState();

                // render game state
                renderGameState();
        }

        // exit
        endGame();
}