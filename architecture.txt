int main()
{
        // game loop
        while (true)
        {
                // take user input
                Event userInput = handleUserInput();

                // handle event
                handleEvent(userInput);

                // update game state
                updateGameState();

                // render game state
                renderGameState();
        }
}