- Finish displayBanner(). Make sure it separates each word in the banner into separate words and then centers each word.
        Perhaps add tool/gui.cpp to add a place where to dump these helper functions.
- Create a simple demo where you can safely move across the app and have the basic GUI elements working.