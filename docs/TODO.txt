Goal: Create a simple demo where you can safely move across the app and have the basic GUI elements working.

- Make the startApplication() function format header and footer and put it formatted into header.txt and footer.txt
- Create the few basic scenes
- Add user input